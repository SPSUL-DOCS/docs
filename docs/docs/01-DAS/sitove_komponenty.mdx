---
sidebar_position: 2
title: Síťové komponenty a služby
---

# Síťové komponenty

## Role hosta

Všechny počítače, které jsou připojeny k síti a přímo se účastní síťové komunikace, jsou klasifikovány jako **hostitelé**.

Hostitelé mohou být koncové zařízení. Některým hostitelům se také říká klienti. Termín hostitelé označuje konkrétně zařízení v síti, kterým je pro účely přidělena IP adresa. Ip adresa identifikuje hostitele a síť, ke které je hostitel připojen.

<!-- zkontrolovat učitelem-->

## Klient-server architektura 

Klient-server je síťová architektura, která odděluje klienta a server, kteří spolu komunikují přes počítačovou síť => server a klient jsou rozdeleni => možnosti serveru lze používat na více počítačích
 
# Síťové služby

## Peer-to-peer (rovný s rovným)

Uživatel sdílí jako **server** a zároveň přímá data jako **host**
- příklad **torrenty** = uživatel stáhne soubor a zasílá části dalším uživatelům

## 