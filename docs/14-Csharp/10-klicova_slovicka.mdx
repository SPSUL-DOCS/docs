---
title: "Klíčová slovíčka a modifikatory přístupu"
description: "Klíčová slovíčka a modifikatory přístupu"
toc_max_heading_level: 2
---

### Enum

- hodnoty si určíme, vytvoření vlastního datového typu

```csharp
Operator znamenko;

enum Operator {
    scitani,
    odcitani,
    nasobeni,
    deleni
}
```

:::tip datový typ Operator, má 4 možné hodnoty
scitani,
odcitani,
nasobeni,
deleni
:::

## Modifikátory přístupu

|  Modifikator  | Přístup                                         |
| :-----------: | :---------------------------------------------- |
|  **Private**  | Vidí jenom daná třída                           |
|  **Public**   | Vidí všechny třídy v jednom projektu            |
|   **Final**   | Nelze od ní dědit                               |
| **Internal**  | <!-- Musim se zeptat učitele -->                |
| **Protected** | Vidí pouze ta třída, která dedí z třídy původní |

### Virtual

- Má vlastní tělo, jde přepsat v jiné **třídě**

```csharp
class Trida1 {
    protected virtual Metoda {
        // tělo metody
    }
}
```

```csharp
class Trida2 : Trida1 {
    override Metoda {
        // přepsaná metoda
    }
}
```

### Abstract

- Nemá tělo, tudíž je povinnost jej **přepsat**

```csharp
class Trida1 {
    protected abstact Metoda;
}
```

```csharp
class Trida2 : Trida1 {
    override Metoda {
        // Přepsaná metoda
    }
}
```

### Override

- Přepisuje virtuální a abstraktní metody/třídy

```csharp
class Trida1 {
    protected virtual Metoda {
        // tělo metody
    }
}
```

:::tip Pokud chceme, aby se při přepisování virtuálí metody volala i původní metoda, použijeme klíčové slovo **base**

```csharp
class Trida2 : Trida1 {
    override Metoda {
        base.Metoda();
        // Přepsaná metoda
    }
}
```

:::
